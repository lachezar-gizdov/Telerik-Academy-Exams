
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PackageManager.Commands;
using PackageManager.Core.Contracts;
using PackageManager.Models.Contracts;
using Telerik.JustMock;

namespace PackageManager.Tests.Commands
{
    [TestClass]
    public class InstallCommandExecuteShould
    {
        //[TestMethod]
        //public void CallOperationFromTheInstaller()
        //{
        //    //Arrange
        //    var installerMock = Mock.Create<IInstaller<IPackage>>();
        //    var packageMock = Mock.Create<IPackage>();
        //    var sut = new InstallCommand(installerMock, packageMock);

        //    Mock.Arrange(() => sut.Execute());

        //    sut.Execute();

        //    Mock.Assert(() => sut.Execute(), Occurs.Once());
        //}
    }
}
